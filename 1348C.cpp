#include<bits/stdc++.h>
using namespace std;
#define ll long long

ll arr[1005];

int main()
{
    ll n,i,t;
    cin>>t;

    while(t--)
    {

    }
    return 0;
}
 if(dist.size()==k)
        {
            cout<<k*dist.size()<<endl;
        }
        else
        {
            ll x= k - dist.size();
            ll z = k*dist.size();
            z += (x*k);
            cout<<z<<endl;
        }
